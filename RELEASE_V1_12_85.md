# Market Cycle Trader Frontend v1.12.85

## Administration workspace refresh

- Rebuilds Administration as one compact workspace consistent with Portfolio, System Settings, Backtest and Analytics.
- Keeps summary, invitation creation, access records and audit activity inside one visual surface separated by internal dividers.
- Moves Refresh into the Administration workspace header.
- Adds contextual hints beside access metrics, invitation fields and sortable table columns.
- Adds client-side search, filters, sorting and pagination to identity-bound invitations.
- Adds client-side search, role/result filters, sorting and pagination to access history.
- Combines Identity-bound invitations and Access history as internal data tabs so long audit history no longer consumes the page below the access records.
- Does not change authentication rules, invitation semantics, API contracts or strategy visibility.

API remains v1.13.26.
