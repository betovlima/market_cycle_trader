# Frontend v1.12.74 — Backtest strategy identity

- Shows the selected test name in the Backtest page header.
- While a job is running, shows the exact strategy snapshot being evaluated.
- When viewing a result, shows the strategy name stored with that job.
- Adds a Test column to Backtest History.
- Does not expose strategy parameters, asset universe, revisions, hashes or model internals.
