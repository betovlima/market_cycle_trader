# Frontend v1.12.75 — Detailed XGBoost progress

- Displays the current run, walk-forward fold, training phase, fitted-model count and compute device in the Backtest execution panel.
- Retains only sanitized fold/training milestones in the visible execution log.
- Keeps asset names, seeds, features, scores, thresholds, model parameters and tracebacks restricted to the protected API console.
- Updates the visible frontend version badge to `v1.12.75`.
