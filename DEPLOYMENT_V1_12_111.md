# Frontend v1.12.111 deployment

Deploy only after API v1.13.42. No environment-variable changes are required. After deployment, refresh the Administrator session and confirm that `SELECTED STRATEGY` shows the model selector and Candidate/Winner model labels.
