[build]
builder = "RAILPACK"
buildCommand = "corepack enable && pnpm install --no-frozen-lockfile --prefer-offline && pnpm run build"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 300
startCommand = "pnpm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5