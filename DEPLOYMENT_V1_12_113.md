# Front v1.12.113 / API v1.13.43

Frontend-only release. Deploy the frontend after the currently validated API v1.13.43. No API or MongoDB migration is required.

## Git

```bash
git checkout develop
git pull
git checkout -b feature/1.12.113-portfolio-measurement-ruler
# replace frontend files with this release
git add .
git commit -m "feat(portfolio): add two-point measurement ruler"
git push -u origin feature/1.12.113-portfolio-measurement-ruler
```
